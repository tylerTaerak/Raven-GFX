package assets

import "shared:raven-gfx/api"
Font :: struct {
}

load_font_data :: proc(
	device : api.Device,
	store : Asset_Store,
	data : []byte) -> (font : []Font, ok : bool = true) {

	return
}
